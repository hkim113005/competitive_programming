#include <iostream>

using namespace std;

int n, m, o1, o2, d[50];

void process(int a, int b, int c, int tot) {
    d[a]
}

int main() {
    cin >> n >> o1 >> o2 >> m;

    for (int i = 0; i < m; i++) {
        cin >> d[i];
    }

    return 0;
}
